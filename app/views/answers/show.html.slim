= render 'shared/errors', resource: @answer
h3 Answer body:
p= @answer.body
= link_to 'Show the question', question_path(@answer.question)

= link_to 'Delete', answer_path(@answer), method: :delete if current_user&.author?(@answer)